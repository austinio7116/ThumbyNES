/* ThumbyNES stripped-down stub for osd_keyboard.h.
 * The original header belonged to the SDL/Linux/Haiku frontend we
 * don't vendor. A few engine .c files still #include it — keep the
 * header empty so those translation units compile; anything they
 * reference from the old contents needs a local declaration or a
 * separate guard. */
#pragma once
